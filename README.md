### Self signed cert

Create a cert, refer to Sy's instruction here https://github.com/synle/self-signed-certificate-notes
`npm start` serves the app over HTTPS (`HTTPS=true`) — trust the self signed cert in your browser first.
